grammar None_grammar is
    main_rule <- Example(@Example)

end
class FooNode is
end

class Example : FooNode is

    public fun foo (): Entity[Example] =
        (if (true) then self else self) then (s => s).as_bare_entity

end

class FooNodeBaseList : FooNode is
end


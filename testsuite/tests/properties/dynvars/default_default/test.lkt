from common import RootNode, v

@abstract class AbstractNode: RootNode {
    @abstract fun prop(): Int
    @exported fun use_prop(): Int = {
        bind v = 10;
        val _ = v;
        node.prop()
    }
}

class ConcreteNode: AbstractNode {
    fun prop(): Int = 12
}

grammar None_grammar {
    main_rule <- Example("example")

}

class FooNode {
}

class Example : FooNode {

    @export fun foo (): Entity[Example] =
    (if (true) then self else self).do((s) => s).as_bare_entity
}

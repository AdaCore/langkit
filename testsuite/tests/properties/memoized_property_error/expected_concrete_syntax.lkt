grammar None_grammar {
    main_rule <- Example(@Example)

}

class FooNode {
}

class Example : FooNode {

    public memoized fun prop1 (): Boolean = {
        val _ = [self]

        raise PropertyError('Explicit error 1')
    }

    public memoized fun prop2 (): Boolean = raise PropertyError('Explicit error 2')
}

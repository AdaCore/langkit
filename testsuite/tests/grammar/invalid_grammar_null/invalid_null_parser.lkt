# Check that GrammarNull has to have a node type as argument when used in
# parsers.

from lexer_example import foo_lexer

@with_lexer(foo_lexer)
grammar foo_grammar {
    @main_rule rule <- null
}

@abstract class FooNode implements Node[FooNode] {
}


# There is currently no intent to support this. This is mostly to verify that
# no error occurs which would prevent code analysis.
val identity : Int = ((i) => i)(1)
val half_decl_type : (Int, Int) -> Int = (i: Int, j) => i + j
val no_decl_type : (Int, Int) -> Int = (i, j) => i + j

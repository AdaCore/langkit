from common import A, B, C, D

fun take_b(v: NodeBuilder[B]): Bool = true

val v1 = take_b(null[NodeBuilder[A]])
val v2 = take_b(null[NodeBuilder[B]])
val v3 = take_b(null[NodeBuilder[C]])
val v4 = take_b(null[NodeBuilder[D]])

with "libfoolang";
with "libbarlang";
with "langkit_support";

project Main is
   for Object_Dir use "obj";
   for Main use ("main.adb");

   package Compiler is
      for Default_Switches ("Ada") use
        ("-g", "-O0", "-gnata", "-gnatwae", "-gnatyg", "-gnatX");
   end Compiler;
end Main;

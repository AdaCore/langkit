grammar None_grammar is
    main_rule <- Example(@Example)

end
class FooNode is
end

class Example : FooNode is

    public memoized fun unit_root_node (unit : InternalUnit): Entity[FooNode] = unit.root.as_bare_entity
end

class FooNodeBaseList : FooNode is
end


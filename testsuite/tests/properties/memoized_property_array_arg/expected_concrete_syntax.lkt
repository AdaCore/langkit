grammar None_grammar is
    main_rule <- Example(@Example)

end
class FooNode is
end

class Example : FooNode is

    public memoized fun get_array (): Array[Integer] = [1, 2]

    public memoized fun test_prop (numbers : Array[Integer], c : Array[CharacterType]): Integer =
        if (c == 'one') then (numbers?[0]) else (numbers?[1])


    public memoized fun test_prop2 (numbers : Array[Entity[FooNode]]): Boolean = numbers.length == 0
end


test.py:XXX: warning: Specified type is FooNode, but it could be more specific: FooNodeBaseList
Code generation was successful
== Doc for Holder.a_list_or_b_list ==
This field can contain one of the following nodes:

* ${node_name(T.A.list)}
* ${node_name(T.B.list)}

== Doc for Holder.a_or_b ==
This field can contain one of the following nodes:

* ${node_name(T.A)}
* ${node_name(T.B)}

== Doc for Holder.a_or_b_list ==
This field contains a list that itself contains one of the following nodes:

* ${node_name(T.A)}
* ${node_name(T.B)}

== Doc for Holder.special_a_list ==
This field contains a list that itself contains one of the following nodes:

* ${node_name(T.A)}

== Doc for Holder.special_a_list_or_special_b_list ==
This field contains a list that itself contains one of the following nodes:

* ${node_name(T.A)}
* ${node_name(T.B)}

== Doc for Holder.special_a_or_b_list ==
This field contains a list that itself contains one of the following nodes:

* ${node_name(T.A)}
* ${node_name(T.B)}

Done

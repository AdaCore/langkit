import lexer_example

@with_lexer(foo_lexer)
grammar foo_grammar {
    @main_rule main_rule <- Example("null")
}

@abstract class FooNode implements Node[FooNode] {
}

class Example : FooNode {
    fun invalid_expr() : Int = from [1,2] match _ if true
}

grammar None_grammar {
    main_rule <- Example(@Example)

}

class FooNode {
}

class Example : FooNode {

    @export fun test (a : Array[InternalUnit]): Array[InternalUnit] =
    <unique(<Var a>)>
}

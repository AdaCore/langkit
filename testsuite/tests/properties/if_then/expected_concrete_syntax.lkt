grammar FooGrammar {
    @main_rule main_rule <- Example("example")

}

@root_node class FooNode {
}

class Example : FooNode {

    @export fun foo (): Example =
    (if (true) then node else node).do((s) => s).as_bare_entity
}

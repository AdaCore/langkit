grammar None_grammar is
    main_rule <- Example(@Example)

end
class FooNode is
end

class Example : FooNode is

    public fun prop1 (arg : Boolean = true): Boolean = arg

    public fun prop2 (arg : Entity[FooNode] = null): Entity[FooNode] = arg
end


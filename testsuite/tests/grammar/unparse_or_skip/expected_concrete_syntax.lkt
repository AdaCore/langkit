import lexer_example
grammar foo_grammar {
    @main_rule main_rule <- list+(or(
        | DefNode("def" identifier "=" number ";")
        | skip(ErrorDef)
    ))
    number <- Number(@number)
    identifier <- Identifier(@identifier)

}

@abstract class FooNode implements Node {
}

class DefNode : FooNode {
    @parse_field name : Identifier
    @parse_field value : Number
}

class ErrorDef : FooNode {
}

class Identifier : FooNode implements SymbolNode {
}

class Number : FooNode implements SymbolNode {
}

grammar foo_grammar {
    @main_rule main_rule <- Root(or(
        Root("example" ASTList[FooNode]+(leaf, ","))
        | ASTList[FooNode]+(leaf)
    ))
    leaf <- or(Identifier(@identifier) | Number(@number))
}

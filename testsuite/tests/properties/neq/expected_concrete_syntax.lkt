grammar FooGrammar {
    @main_rule main_rule <- list*(Example("example"))

}

@root_node class FooNode {

    @export fun integers_neq (a : Int, b : Int): Boolean = not (a = b)
}

class Example : FooNode {

    @export fun not_eq (other : Example): Boolean = not (node = other)
}

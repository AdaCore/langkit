grammar None_grammar is
    main_rule <- Example(@Example)

end
class FooNode is
end

class Example : FooNode is

    public fun test (a : Array[InternalUnit]): Array[InternalUnit] = <unique(<Var a>)>
end

